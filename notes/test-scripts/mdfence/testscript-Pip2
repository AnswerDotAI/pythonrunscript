#!/usr/bin/env pythonrunscript
#
# ```requirements.txt
# tqdm==4.66.4
# ```
#
# ```requirements.txt
# ```requirements.txt
# pyyaml==6
# ```

from tqdm import tqdm
import sys

print("I'm the test script")
print("I'm the test script's second line")

print(f"test script args: {sys.argv}")
for i in tqdm(range(10000)):
    pass


    
